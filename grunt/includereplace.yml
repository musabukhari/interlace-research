html:
  options:
    prefix: '<!-- @'
    suffix: ' -->'
    includesDir: '<%= client_dir %>/includes'
  files:
    - src: '**/*.html'
      dest: '<%= build_dir %>/'
      expand: true
      cwd: '<%= src_dir %>/'
